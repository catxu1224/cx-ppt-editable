---
name: ppt-editable
description: 将图片型 PPTX、整页 PNG/JPG、截图或扫描幻灯片按原图 1:1 重建为可编辑 PowerPoint。适用于严格复刻版式、文字、配色、图标、形状和线条，并要求所有对象保持顶层独立可编辑的任务；不用于根据提纲重新设计演示文稿。
---

# PPT 1:1 可编辑复刻

将现有幻灯片图片逐页重建为视觉高度一致、结构真实可编辑、可在 PowerPoint 中继续框选和组合的 PPTX。目标是 1:1 复刻，不是重新设计、近似排版或套用模板。

## PNG/JPG 截图复刻的实战约束

以下规则来自连续逐页复刻和 PowerPoint 实际渲染校验，优先级高于任何自动美化、重排或内容精简策略：

### 源图是唯一布局基准

- 完全按照当前源图的页面比例、裁切范围、元素坐标、宽高、间距、留白、层级和换行复刻，不得因为“更美观”“更规整”“更易读”而移动、放大、缩小、补齐或重排任何区域。
- 同一页面的不同截图若比例、裁切或清晰度不同，应按用户当前指定的截图单独校准；不得直接复用旧坐标后宣称完成。
- 先锁定页面尺寸和所有主要区域的边界，再处理字体与细节。文字溢出时优先调整形状内字号、内边距和自动换行，不得擅自改变形状尺寸或整体布局。

### 文字零删减与语义分组

- 源图中除水印外的所有可见文字必须逐字保留，包括标题、正文、括号、标点、数字、星号、脚注、表头、空值符号和形状内标签；不得摘要、改写、合并、补写或删除。
- OCR 只用于辅助定位和识别，必须结合源图人工复核。清晰度不足的文字应标记为待复核，不得凭经验静默删减。
- 按语义对象建文本框，而不是按视觉行建文本框。一个标题、一个自然段、一个项目符号条目、一段卡片说明或一个表格单元格中的连续文字，应分别保留在一个文本框、一个形状文本框或一个单元格内。
- 严禁把同一段文字按每一行、每几个字或每个颜色片段拆成多个叠放文本框。段内不同颜色、粗细或字号必须通过同一段落中的多个 run 实现。
- 项目符号和多级列表必须使用 PowerPoint 原生段落格式，并恢复左缩进、悬挂缩进、层级、段前段后和行距；不得把圆点、方框或序号写成普通正文字符。

### 字体和颜色必须忠实保留

- 逐对象、逐段落、逐 run 复刻字体、字号、字重、颜色、对齐和行距。黑色、红色、蓝色、白色等重点文字不得统一成单一颜色。
- 字体替换仅在源字体不可用时进行，优先选择字形宽度和视觉重量最接近的字体，并通过 PowerPoint 渲染重新校准换行。
- 不得为了减少工作量把整段文字统一加粗、统一改色或统一字号；也不得使用自动主题色替代源图中的明确 RGB 颜色。

### 禁止阴影与边界兜底

- 转换出来的所有元素不得包含阴影，包括文本框、矩形、圆角矩形、卡片、图片、表格、图标和标题条；即使源图看起来存在投影，也统一去除阴影。
- 如果不加阴影会导致元素和页面底色完全融为一体，仅允许为该元素添加 `0.5 像素`的浅灰色实线边框（约 `0.375 pt`），不得使用更粗边框、深色边框、发光或阴影替代。
- 输出前必须检查并报告 `shadows=0`；若使用了边界兜底，应在编辑性报告中列出使用浅灰色边框的对象。

### 水印必须识别为 omit，而不是内容

- 对重复斜排、低透明度、跨越多个区域的机构名、编号、日期、保密提示等水印进行视觉识别，并在 OCR 审核记录中标记为 `omit`。
- 不得把水印转换成文字、形状、背景、图片裁片或任何可见对象；最终 PPTX 和渲染预览均不得出现水印残影。
- 若照片或复杂背景被水印覆盖，不得直接携带水印裁入 PPTX。应使用无水印原图、无水印近似背景或重新生成的干净图片，并在交付说明中注明该图片替换。

### 组织架构图和流程图

- 先按源图固定所有矩形框、圆角矩形、标签和节点的坐标与尺寸，再处理框内文字。文字必须直接写入对应形状内部。
- 对长名称或多行标签，通过调整框内字号、自动换行、行距和内边距适配；不得把文字移到独立文本框覆盖在矩形上，也不得为容纳文字擅自放大矩形导致架构布局变化。
- 箭头、树状分支和组织关系必须使用 PowerPoint 原生连接符或连接线真正建立连接。连接线端点应吸附到相关形状或明确的分支节点，移动节点时连接关系仍应保持。
- 不得用彼此断开的普通短线在视觉上伪装连接关系。连接线应位于节点形状下层，复刻源图的颜色、粗细、虚实、折点和箭头样式，并在渲染后检查是否出现悬空、穿框或错接。

### 原生表格和高密度页面

- 具有明确行列语义的内容必须重建为一张原生 PowerPoint 表格，保留合并单元格、列宽、行高、边框、底色和单元格内文字格式；不得拆成散落线条与文本框。
- 高密度表格先匹配整体表格边界和行列尺寸，再校准单元格字号与换行。不得通过删除文字或把表格整体改版来解决溢出。
- 表格中的颜色强调、勾选符号、斜杠、空白单元格和方案差异均属于内容，必须保留。

### 渲染校验顺序

每页必须用 PowerPoint 或 LibreOffice 实际渲染为与源图同分辨率的 PNG，并按以下顺序比对和修正：

1. 页面比例与裁切；
2. 大区域边界、坐标和留白；
3. 表格、卡片、组织架构和连接关系；
4. 字体、颜色、字号、字重和对齐；
5. 段落语义分组、换行、行距和缩进；
6. 文字完整性与水印清除；
7. `groups=0`、`placeholders=0`、`noGrp=0` 结构审计。

只要存在明显布局变化、文字删减、同段逐行拆框、形状内文字溢出、连接线未真正连接、水印残留或任何阴影，均不得标记为完成。

## 强制交付标准

以下要求是不可绕过的验收门槛：

1. 按源图复刻页面比例、坐标、尺寸、层级、留白、配色、字体、字号、字重、对齐、换行、图标、形状、线条、表格、图片和页码。
2. 所有文字、形状、线条、连接符、图片和图标均为幻灯片画布上的顶层独立对象。
3. 不预先分组，不得存在 PowerPoint 组合对象。
4. 不使用占位符，不依赖母版占位符承载可见内容。
5. 不添加透明蒙层、透明热区或用于假装可编辑的覆盖对象。
6. 不设置禁止组合锁；OOXML 中不得出现 `noGrp="1"` 或等效配置。
7. 必须允许用户在 PowerPoint 中自行框选任意对象并组合。
8. 不得把整页源图作为背景，再在上面覆盖可编辑文字或透明对象。
9. 照片、截图、复杂插画或无法原生表达的视觉区域可保留为普通图片对象，但必须紧密裁切，不得携带重复文字或覆盖无关区域。
10. 所有元素一律不使用阴影、发光、柔化、倒影、立体或主题效果；若元素与底色完全融为一体，仅允许添加 `0.5 像素`浅灰色实线边框。

输出前必须检查并报告：

- `groups=0`
- `placeholders=0`
- `noGrp=0`

同时必须检查并报告 `shadows=0`。如使用 `0.5 像素`浅灰色边框兜底，应在编辑性报告中说明具体对象。

## 文字必须属于其图形

当文字位于矩形、圆角矩形、圆形、箭头、流程块、标签、徽章、表格单元格或其他可承载文字的元素内时：

- 必须把文字直接写入该形状或单元格的文本框架中。
- 不得另外创建独立文本框覆盖在图形上。
- 上、下、左、右文本内边距默认均设为 `0.1 cm`。
- 根据源图设置水平和垂直对齐。
- 开启自动换行。
- 开启适配形状的自动文字调整，优先使用“溢出时缩小文字”或等效设置。
- 自动调整后仍须检查字号、行距和断行，不得出现单字孤行、标点孤行、裁切或明显偏离源图的换行。
- 只有纯标题、页脚、来源、注释或没有承载图形的自由文字才使用独立文本框。

若图形本身需要独立选择，但文字必须随图形移动，仍应使用形状内文字，不得以叠放文本框替代。

## 行距与项目符号

### 行距

- 单行标题统一设置为 `1.0 倍行距`。
- 多行标题、多行正文及其他多行文本统一设置为 `1.2 倍行距`。
- 行距规则适用于独立文本框、形状内文字和表格单元格文字。
- 设置行距后仍须检查文本框高度、形状高度、段前段后间距、自动换行和自动缩小效果，避免文字溢出或与源图位置产生明显偏差。

### Bullet point

- 源图存在项目符号列表时，必须使用 PowerPoint 原生 bullet point 段落格式。
- 不得把 `•`、`·`、`●`、`○`、`-`、`—` 等标点或字符直接写入正文来冒充项目符号。
- Bullet 段落默认设置为 `Before text: 0.3 cm`、`Hanging: 0.3 cm`。
- Bullet 符号、颜色、字号和层级应尽量匹配源图；每条内容必须是独立段落，不得通过手工空格对齐。
- 多级项目符号应使用 PowerPoint 原生列表层级，并分别检查缩进、悬挂、换行对齐和行距。

## 表格单元格边距

当源页面包含表格时，必须使用原生 PowerPoint 表格并对所有单元格应用以下默认文本内边距：

- 左边距：`0.1 cm`
- 右边距：`0.1 cm`
- 上边距：`0.05 cm`
- 下边距：`0.05 cm`

表头、正文、合并单元格和空白单元格均执行同一默认值，除非源图为了 1:1 复刻明确需要不同边距。设置边距后必须重新检查自动换行、行高、列宽、垂直对齐和文字溢出，不得仅通过缩小字号掩盖表格尺寸偏差。

## 图标与非标准元素

图标是复刻内容，不得随意省略。按以下优先级处理：

1. 在 PowerPoint 原生图形库或可用 SVG 图标库中寻找语义、轮廓、线宽和视觉重量接近的替代，并匹配源图颜色、尺寸和方向。
2. 找不到足够相似的图标或元素时，从源图精确抠图，去除背景和无关像素，作为普通顶层图片对象插入。
3. 抠图必须紧贴元素边界，不得包含邻近文字、线条、底色、卡片边框或其他对象。
4. 不得用 emoji、字体符号、风格明显不一致的通用图标或粗糙近似图形替代。
5. 图标内部若含需编辑文字，应分离处理：图标保留为图片或矢量对象，文字重建为原生文字，不得保留重复文字残影。
6. 每个替代或抠图对象均保持顶层独立，不得分组或锁定。

## 元素复刻策略

逐页、逐区域选择方式，不强迫整套文件使用同一路线：

| 源图元素 | 首选方式 |
|---|---|
| 标题、正文、标签、数字 | 原生可编辑文字 |
| 卡片、矩形、圆形、徽章、箭头、分隔线 | PowerPoint 原生形状或连接符 |
| 真实行列表格 | 原生 PowerPoint 表格 |
| 图表且数据可可靠恢复 | 原生可编辑图表 |
| 照片、产品截图、复杂插画 | 普通图片对象，紧密裁切 |
| 复杂图标或图库外元素 | 相似矢量替代；找不到时精确抠图 |
| 渐变、纹理或复杂背景 | 不携带源文字的紧密背景裁片或原生填充 |

视觉保真优先，但不得通过整页截图、重复文字或透明覆盖牺牲真实可编辑性。

## 执行流程

### 1. 输入与环境检查

- 确认源文件、页数、页面比例、图片分辨率和目标输出范围。
- 检查 OCR、图片裁切、PPTX 生成和 PowerPoint/LibreOffice 渲染能力。
- 多页输入逐页建立任务和 QA 记录，不把总览图误认为正式页面。

### 2. OCR 与页面拆解

- 保留未经修改的源图。
- 对每页运行 OCR，记录文本、置信度、方向、行框和区域框。
- OCR 结果必须审核，分别标记为 `accepted`、`corrected`、`needs_review`、`omit`、`keep_in_picture` 或 `reconstruct_as_native`。
- 拆解标题区、正文区、图表区、表格区、图标、线条、连接符、图片、页脚和背景。
- 记录源坐标、尺寸、颜色和前后层级，形成逐页重建计划。

### 3. 原生重建

- 先重建大结构、背景色块、网格与主分区，再重建细节。
- 规则几何元素优先使用原生形状。
- 连接关系使用原生线条或连接符，复刻粗细、颜色、虚实、端点和箭头样式。
- 表格使用原生表格并保留行列语义，不得仅用线条和散落文本伪造；所有单元格默认采用左右各 `0.1 cm`、上下各 `0.05 cm` 的文本内边距。
- 文本按源图坐标、字体、字号、颜色、字重、行距和换行恢复。
- 有承载图形的文字写入图形内部，并应用 `0.1 cm` 四边内边距、自动换行和自动适配。
- 单行标题采用 `1.0 倍行距`；多行标题、正文及其他多行文本采用 `1.2 倍行距`。
- 项目符号列表使用 PowerPoint 原生 bullet point，默认设置 `Before text: 0.3 cm`、`Hanging: 0.3 cm`，不得使用标点字符伪造。
- 图片和抠图对象保持普通图片对象，可独立移动、缩放、替换和裁切。

### 4. 视觉比对

- 用 PowerPoint 或 LibreOffice 将 PPTX 渲染为 PNG。
- 将渲染图与源图逐页对照，检查位置、尺寸、色差、字体、换行、线条、图标、表格、留白和层级。
- 结构化页面不得仅凭“文件可打开”或“对象数量大于零”判定成功。
- 明显偏差必须修订后重新渲染。

### 5. 结构审计

审计整个 PPTX，包括幻灯片、母版和版式：

- 组合对象总数必须为 0。
- 占位符总数必须为 0；删除占位符形状本体，而非只删除 `<p:ph>` 标记后留下空形状。
- 所有 `noGrp` 属性必须清除，最终计数为 0。
- 检查整页图片伪装、重复文字、透明蒙层、不可见覆盖对象和异常锁定。
- 检查形状内文字的四边内边距、自动换行、自动适配和溢出情况。
- 检查单行标题是否为 `1.0 倍行距`，多行文本是否为 `1.2 倍行距`。
- 检查项目符号是否为 PowerPoint 原生 bullet point，并核对 `Before text: 0.3 cm`、`Hanging: 0.3 cm`，不得存在以普通字符伪造的 bullet。
- 检查原生表格所有单元格的文本内边距：左右各 `0.1 cm`、上下各 `0.05 cm`，并检查行高、列宽、换行和溢出。
- 检查图片裁切是否紧密，是否携带重复文字或邻近元素。

## 必须保留的质量证据

- 未修改的源图；
- OCR 结果及审核记录；
- 逐页重建计划；
- 图标替代与抠图清单；
- PowerPoint/LibreOffice 渲染预览；
- 编辑性报告；
- 结构审计结果；
- 逐页视觉 QA 结论。

编辑性报告至少列出每页顶层对象数、原生形状数、可编辑文字数、图片数、表格数、连接符数，以及 `groups`、`placeholders`、`noGrp` 的最终计数。

## 拒收条件

出现以下任一情况不得标记为完成：

- 原图文字仍可见，同时叠加可编辑文字；
- 用整页截图充当可编辑复刻；
- 结构、配色、图标或页面比例与源图明显不一致；
- 规则形状被粗糙截图替代；
- 图标被省略、错误替代，或抠图包含周边内容；
- 图形内文字被拆成覆盖其上的独立文本框；
- 文字溢出、裁切、单字孤行或换行明显漂移；
- 单行标题未使用 `1.0 倍行距`，或多行文本未使用 `1.2 倍行距`，且没有记录源图要求的合理例外；
- 使用标点字符代替 PowerPoint 原生 bullet point，或 bullet 段落未按默认值设置 `Before text: 0.3 cm`、`Hanging: 0.3 cm`；
- 表格被拆成无语义线条和文本；
- 表格单元格未按默认值设置左右各 `0.1 cm`、上下各 `0.05 cm` 的文本内边距，且没有记录源图要求的合理例外；
- 存在组合、占位符、透明蒙层或禁止组合锁；
- `groups`、`placeholders`、`noGrp` 任一不为 0；
- 有条件却未实际用 PowerPoint 或 LibreOffice 打开并渲染；
- 缺少编辑性报告或逐页视觉 QA。

## 交付说明

最终回复必须说明：

- 输出 PPTX 的路径和页数；
- 哪些元素为原生可编辑对象；
- 哪些复杂元素保留为普通图片对象；
- 是否使用图标替代或抠图；
- PowerPoint/LibreOffice 渲染结果；
- `groups=0`、`placeholders=0`、`noGrp=0` 的检查结果；
- 仍存在的限制或需要人工复核的内容。
